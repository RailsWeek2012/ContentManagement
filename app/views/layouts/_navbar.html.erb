<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="#">Vereins CMS</a>
      <ul class="nav pull-left">
        <%= raw params[:controller] == "events" ? "<li class='active'>" : "<li>" %>
               <% if user_signed_in? %>
                    <%= link_to "logout", logout_path, method: :delete %>
               <% else %>
                    <%= link_to "login", login_path  %>
               <% end %>
          </li>
      </ul>
      <ul class="nav pull-left">
        <%= raw params[:controller] == "events" ? "<li class='active'>" : "<li>" %>
        <% if user_signed_in? %>
            <%= link_to "Neues Login anlegen", new_user_path %>
        <% end %>
        </li>
      </ul>
      <ul class="nav pull-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <% if user_signed_in? %>
              Max Mustermann
            <% else %>
              Navigation
            <% end %>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to "managements" do %>
                <i class="icon-user"></i> Vorstand
              <% end %>
            </li>
            <li>
              <%= link_to "newsscripts" do %>
                <i class="icon-th-list"></i> News
              <% end %>
            </li>
            <!--
            icon-ok, icon-remove, icon-off
            -->
          </ul>
        </li>
      </ul>
    </div>
  </div>
</div>